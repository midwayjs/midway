service: serverless-midway-test

provider:
  name: aws

functions:
  index:
    handler: index.handler
    events:
      - http:
          method: get
  index2:
    handler: index.handler2
    events:
      - http:
          method: get
