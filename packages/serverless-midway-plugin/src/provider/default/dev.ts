import { CommandBase, ICommand, IHooks } from '../../core/commandBase';
export class Dev extends CommandBase {

  getCommand(): ICommand {
    return {};
  }

  getHooks(): IHooks {
    return {};
  }
}
